# Azure OpenAI 설정
# 실제 키를 여기에 채우고, 파일명은 .env 로 복사하여 사용
AZURE_OPENAI_ENDPOINT=https://aaa6mvccb3u75v2-cog-ac.openai.azure.com/
AZURE_OPENAI_API_KEY=YOUR_AZURE_OPENAI_API_KEY
AZURE_OPENAI_CHAT_API_VERSION=2024-12-01-preview
# 배포(디플로이먼트) 이름: 포털에서 생성한 모델 리소스의 배포 이름을 넣어야 함
AZURE_OPENAI_CHAT_DEPLOYMENT=gpt-5.4
AZURE_OPENAI_EMBED_DEPLOYMENT=text-embedding-3-large

# SurrealDB 접속 정보(현재 코드는 기본값을 하드코딩: ws://localhost:8000, root/root)
# 추후 코드에서 환경변수로 분리 예정
SURREALDB_URL=localhost:8000
SURREALDB_USERNAME=root
SURREALDB_PASSWORD=root
SURREALDB_NS=namespace
SURREALDB_DB=database

# JWT 설정 (MVP용 샘플)
JWT_SECRET=dev_secret_change_me
ACCESS_TOKEN_TTL_SECS=3600
REFRESH_TOKEN_TTL_SECS=604800

# 파일 업로드 디렉터리
UPLOAD_DIR=./uploads
