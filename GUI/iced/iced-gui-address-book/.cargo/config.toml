# Homebrew LLVM의 ld64.lld는 최신 macOS SDK 오브젝트와 프로젝트의
# 배포 대상 버전을 비교해 불필요한 버전 경고를 출력한다.
# 이 macOS GUI 프로젝트는 Xcode가 제공하는 시스템 링커를 사용한다.
[target.aarch64-apple-darwin]
linker = "clang"
# 상위 사용자 설정의 `-fuse-ld=lld` 뒤에 적용되어 시스템 `ld`를 선택한다.
rustflags = ["-C", "link-arg=-fuse-ld=ld"]
