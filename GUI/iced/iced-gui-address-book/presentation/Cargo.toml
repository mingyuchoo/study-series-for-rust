[package]
name = "presentation"
version = "0.1.0"
edition = "2024"

[dependencies]
domain = { path = "../domain" }
application = { path = "../application" }
infrastructure = { path = "../infrastructure" }
iced = { version = "0.14.0", default-features = false, features = ["tiny-skia", "advanced", "thread-pool"] }
