[package]
  edition = "2024"
  name    = "plugin-manager"
  version = "0.1.0"

[dependencies]
  libloading       = "0.9"
  plugin-interface = {path = "../plugin-interface"}

[lib]
  name = "plugin_manager"
  path = "src/lib.rs"
